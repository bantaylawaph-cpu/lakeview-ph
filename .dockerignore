# Ignore VCS and local artifacts
.git
.gitignore

# Node
node_modules
npm-debug.log*
yarn.lock
pnpm-lock.yaml

# PHP / Composer
vendor
# Keep composer.lock in build context so Docker layer caching and deterministic installs work
# (do NOT ignore composer.lock)
composer.phar

# Build and IDE
.DS_Store
Thumbs.db
.vscode
.idea

# Runtime caches/logs (mounted/persistent in production)
storage/logs/*
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/testing/*

# Docker (keep Dockerfile and .dockerignore available to the builder)
# Do NOT ignore Dockerfile or .dockerignore here

docker-compose.yml
